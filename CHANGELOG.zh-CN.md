# 更新日志

本文件从 v4.2.13 起记录。更早的版本请见 [CHANGELOG.md](CHANGELOG.md)（英文）。

## v4.2.13 — 2026-09-22

上一个公开发布版本是 v4.2.10，因此这一版包含了三个版本的改动。它的跨度比版本号
看上去要大，其中一部分会改变行为。在升级自动化任务之前，请先读第一节。

### 行为变化

**普通的本地复制没有变化。** 不带 `--use-sudo` 的 `blitcp /src /dst` 与 4.2.10
的行为一致，包括目标中的符号链接目录——它们仍然会被跟随，与 `cp -r` 的做法一致。
下面所有内容都只关于提权复制和远程目标。如果你只是以自己的身份在本地复制，
这一节与你无关。

**提权运行时，目标中的符号链接目录会被拒绝。** 当 `/dst/photos` 是一个指向 `/dst`
之外的符号链接时，`blitcp --use-sudo /src /dst` 现在会拒绝 `photos/` 下的文件，
而不是穿过链接写进去。进程是 root，而同一台主机上能写入目标目录的非 root 用户
可以埋下这个符号链接然后等待——正因为他知道一次提权复制即将发生。链接事先就存在
这件事本身就是攻击手法，而不是它无害的证据，并且 blitcp 无法把它与你自己创建的
链接区分开。

**同一条规则现在覆盖所有远程目标**——拉取、推送和中继，无论是否提权，
并且两种传输方式都涵盖：SFTP，以及当服务器关闭 SFTP 子系统时它自行选择的
tar-over-SSH 路径。在 SSH 连接的另一端，「这是你自己的布局」这个说法无人能够核实。
在此之前，中继会拒绝，而推送到逐字节相同的目标却会把五个文件中的四个写到目标之外，
并以 0 退出。

**目标本身是符号链接时会被拒绝**，本地和远程都是如此。当 `/dst` 指向
`/somewhere/else` 时，`user@host:/dst` 不会再去填充 `/somewhere/else`。
对端不会产生任何东西：不建目录，不写清单附属文件，也不做校验。

*这三条的应对办法：* 把真实路径写成目标。像群晖（Synology）那样通过链接访问
`/home` 的服务器，需要把目标路径本身写出来。被拒绝的运行会在写入任何内容之前，
一次性说明是哪个目录、它指向何处、影响多少文件。

### 安全

**通过普通 SSH 的拉取没有校验它解包的内容。** `_tar_extract_stream` 直接对
来自一台你未必控制的机器的内容调用了 `tarfile.extract()`：没有路径穿越检查，
不拒绝符号链接、设备文件或硬链接成员，不检查符号链接祖先目录，也不剥离
setuid/setgid。现在它与其他所有解包路径遵循同一套成员策略。

**tarfile 过滤器的回退路径不再静默穿透。** 在没有 `filter=` 参数的 Python 上，
这段代码走的是一条看起来像回退、实际却不是的路径。

**`--use-sudo` 不再丢失、也不再无条件信任凭据路径。** 指明凭据库的变量会被带过
重新执行，而不是被丢弃；并且在被带过去之前会先做检查：重新执行之后是 *root*
打开那个文件，因此一个别人可改写的凭据库会把提权运行导向另一个 SSH 主机、
另一个密钥路径、另一个存储桶。

### 修复

**一次拒绝了所有文件的运行，仍可能被记录为成功。** 四个引擎写入了一份没有任何
读取方的拒绝列表，于是一次什么都没写的运行会带着成功摘要以 0 退出。摘要现在会
核对自己的算术——copied + linked + refused + skipped 与总数相符，字节数同样闭合——
当它无法自圆其说时会打印警告，而不是给出一个站不住脚的总数。JSON 记录也做同样的
检查，无法闭合的记录会让整次运行失败。

**上传到对象存储时，会跳过目标上并不存在的文件。** 跨运行的跳过判断只依据已签名的
清单——那是关于源、并且关于过去的事实。在两次运行之间删掉一个对象，第二次运行会
永远把它报告为未改变，再也不会把它取回来。现在存在性和大小来自对目标的一次列举。
同一套驱动也服务于 SMB，所以那里有同样的漏洞。

**云端下载每次都重新拉取全部内容。** 下行方向上根本没有增量检查。

**目标支持 reflink 时，`--preserve owner`、`xattr` 和 `acl` 对每个文件都被丢弃。**
在开启 `reflink=1` 的 XFS、btrfs、bcachefs 和 APFS 上，克隆是快速路径，
因此每个文件都走了那唯一一个自带手写元数据代码的分支，五类元数据只应用了两类。
ext4 上的复制从未受影响；如果你带着这些选项复制到了上述文件系统，
请重新运行一次，让元数据落到位。

**拉取会静默丢失属主信息。** 拉取时的 `--preserve owner` 什么都没做也什么都没说，
而逐字节相同的本地复制却会报告 "skipped on N (need root)"。现在属主是按照
实际落地的文件来衡量的，两个方向都是如此。

**提权拉取没有留下任何审计痕迹。** `write_sudo_audit` 只有一个调用点，在本地流程里。
现在全部六条远程流程都会写一条。

**`--log-file` 在四条路径上不产生文件**——仅 SSH 的拉取、推送和中继，
以及所有方向上的 `--dry-run`——并且在另外三条路径上写出的记录没有逐文件条目。

**目标无法保存的文件名会被指名报告。** 含有 `"` 的文件名在 SMB 共享上无法存在；
服务器会拒绝创建，并且不会记录这个名字，而 blitcp 会丢掉这个文件且任何地方
都没有记录。现在它会被报告为「拒绝」，并带上名字。它不改变退出码：
没有任何重试或选项能修好一个共享无法表示的名字。

**指向目录的 HTTP 源 URL 被当成了文件来下载。** `https://host/dir/` 会产生目标
文件名 `dir`，这个名字足够像样，于是运行继续下去并打开了那个 URL，
最终报告的是网络随后说了什么，而不是本该给出的拒绝。

**下载到 SMB 目录的 HTTP 传输可能把目录写成文件。** 当共享上还没有 `dir` 时，
`smb://host/share/dir/` 会在共享根部创建一个名为 `dir` 的*文件*。
那次运行以 0 退出，校验也通过了——因为它读回的正是它刚刚写下的那个文件。

**`--smb-port` 被忽略。** 只要 URL 没有写明端口，URL 解析器就会填入 445，
这在该选项被查询之前就把优先级链条消耗掉了。`--smb-port 1` 照样下载了共享并以 0 退出。

**一项已发布的速度宣称被撤回。** 实际测得的结果见 `benchmark.md`。
