# Optional — only needed for object-storage / SMB transfers
# (s3:// / az:// / gs:// / smb://). The fast_copy.py engine imports these
# lazily; a plain local or SSH copy needs none of them.
boto3>=1.28              # S3 and all S3-compatible (MinIO, R2, Wasabi, B2)
azure-storage-blob>=12.0 # Azure Blob Storage (az://)
google-cloud-storage>=2.0 # Google Cloud Storage (gs://)
smbprotocol>=1.10        # SMB/CIFS shares (smb://host/share and \\host\share)
cryptography>=41.0       # only for an encrypted credentials file (creds encrypt)
